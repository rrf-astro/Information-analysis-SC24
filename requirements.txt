# Reproducibility environment for the SC24 information-theoretic analysis.
# All versions are pinned to those used to produce the frozen results
# (Python 3.12.2). The result tables in this repository were generated with
# exactly this environment.
#
# NOTE: scipy is pinned deliberately. Its Mann-Whitney U implementation has
# changed across releases; a different scipy may shift the diagnostic
# table_min_max.csv (MW_U by a few units) without affecting any reported
# scientific conclusion.

numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
matplotlib==3.9.2
scikit-learn==1.5.1
astropy==6.1.3
astroquery==0.4.11
kneed==0.8.6
tabulate==0.9.0
